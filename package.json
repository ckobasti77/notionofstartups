{
  "name": "notion-clone",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.9"
  },
  "scripts": {
    "dev": "npm run dev --workspace @notion-clone/web",
    "dev:mobile": "npm run start --workspace mobile",
    "dev:backend": "npm run dev --workspace @notion-clone/backend",
    "build": "npm run build --workspace @notion-clone/web",
    "start": "npm run start --workspace @notion-clone/web",
    "lint": "eslint",
    "test": "vitest run",
    "check": "npm run lint && npm run build"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^20",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "postcss": "8.5.23",
    "sharp": "0.35.3"
  }
}
